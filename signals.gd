extends Node

signal play_button_pressed
signal sounds_toggled

signal reset_summoner

signal button_released(button_type)
signal button_pressed(button_type)

signal progress_tutorial

signal get_hurt
signal add_score(amount)

signal game_over
